# daily-planner
Your code stores up to 15 tasks, tracks them with count, and uses a clear menu to add tasks, view one task, view all tasks, or exit. Input is handled with getchar() and fgets(), and task numbers are validated. The switch cleanly manages all planner functions.
